import Config

# Configure the Logger level for tests
config :logger, level: :debug

# You can add other test-specific configurations here.
# For example, if your application uses a database, you might
# configure a test database connection:
# config :my_app, MyApp.Repo,
#   database: "my_app_test.db",
#   pool_size: 5